{
  "branches": ["main", "master"],
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    "@semantic-release/changelog",
    [
      "@semantic-release/exec",
      {
        "publishCmd": "./gradlew build -Pversion=${nextRelease.version} && if ./gradlew -q tasks --all | grep -q '^publish$'; then ./gradlew publish -Pversion=${nextRelease.version}; else echo 'No publish task, skipping publish'; fi"
      }
    ],
    [
      "@semantic-release/github",
      {
        "assets": [
          {
            "path": "build/libs/*.jar",
          }
        ]
      }
    ],
    "@semantic-release/git"
  ]
}
